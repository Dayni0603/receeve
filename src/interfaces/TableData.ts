export interface TableData {
  id: string;
  name: string;
  state: string;
  mobile: string;
  email:string;
  
}

export interface Actions {
  name: string;
  icon?: string;
  class?: string;
  event?: string;
 
}
